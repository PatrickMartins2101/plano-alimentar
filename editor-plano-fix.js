(()=>{
'use strict';
const KEY='planoAlimentar_editorPlano_v3',ID='editorPlanoFinal';
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
function load(){try{return JSON.parse(localStorage.getItem(KEY))||{meals:{},customMeals:[],disabledOriginal:[]}}catch{return{meals:{},customMeals:[],disabledOriginal:[]}}}
function save(x){localStorage.setItem(KEY,JSON.stringify(x))}
function baseMeal(id){const e=document.getElementById(id);if(!e)return null;return{id,title:$('.meal-head h2',e)?.textContent.trim()||id,time:$('.time',e)?.textContent.trim()||'',foods:$$('.food',e).map(f=>{let subs=[];try{subs=JSON.parse($('.swap',f)?.dataset.subs||'[]')}catch{}return{icon:$('.food-icon',f)?.textContent.trim()||'🍽️',name:$('.food-name',f)?.textContent.trim()||'',qty:$('.food-qty',f)?.textContent.trim()||'',subs}}),notes:$('.notes',e)?.innerHTML||''}}
function current(id){const s=load();return s.meals?.[id]||baseMeal(id)}
function css(){if($('#editor-plano-fix-css'))return;const s=document.createElement('style');s.id='editor-plano-fix-css';s.textContent=`#${ID}{max-width:1200px;margin:18px auto;padding:0 20px;font-family:Arial,sans-serif;color:#17352d}#${ID} .ef-card{background:#fffdf8;border:1px solid #d8d5c7;border-radius:20px;padding:20px;box-shadow:0 6px 20px #17352d14}#${ID} h2{margin:0 0 6px}#${ID} .ef-info{background:#eaf4ed;border:1px solid #c9decf;border-radius:12px;padding:12px;margin:12px 0;color:#176b52;font-size:13px}#${ID} .ef-list{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}#${ID} .ef-meal{border:1px solid #dedacf;border-radius:12px;padding:12px;background:#fff}#${ID} button{border:0;border-radius:9px;padding:9px 12px;font-weight:800;cursor:pointer}#${ID} .ef-edit{background:#176b52;color:#fff}#${ID} .ef-soft{background:#e7eee5;color:#176b52}#${ID} .ef-editor{display:none;margin-top:16px;border-top:1px solid #ddd8cc;padding-top:16px}.ef-editor.show{display:block}#${ID} .ef-top{display:grid;grid-template-columns:1fr 150px;gap:10px}#${ID} label{display:grid;gap:5px;font-size:11px;font-weight:800}#${ID} input,#${ID} textarea{width:100%;padding:9px;border:1px solid #d8d5c7;border-radius:8px;font:inherit}#${ID} .ef-food{border:1px solid #e0ddd4;border-radius:12px;padding:10px;margin-top:10px}#${ID} .ef-food-grid{display:grid;grid-template-columns:55px 1fr 180px 35px;gap:8px}#${ID} .ef-sub{margin:8px 0 0 63px;padding:8px;background:#f6f4ec;border-radius:9px}#${ID} .ef-subrow{display:grid;grid-template-columns:1fr 32px;gap:5px;margin-top:5px}#${ID} .ef-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}#${ID} .ef-empty{font-size:12px;color:#71817a}#${ID} .ef-danger{background:#f5dfda;color:#8b3e31}@media(max-width:700px){#${ID}{padding:0 10px}.ef-list,.ef-top{grid-template-columns:1fr}#${ID} .ef-food-grid{grid-template-columns:45px 1fr 35px}#${ID} .ef-food-grid .qty{grid-column:2}#${ID} .ef-sub{margin-left:0}}`;document.head.appendChild(s)}
function food(f){return `<div class="ef-food" data-food><div class="ef-food-grid"><input class="icon" value="${esc(f.icon||'🍽️')}" maxlength="4"><input class="name" value="${esc(f.name)}" placeholder="Alimento"><input class="qty" value="${esc(f.qty)}" placeholder="Quantidade / medida"><button class="ef-danger remove" type="button">×</button></div><div class="ef-sub"><b>🔄 Substituições</b><div class="subs">${(f.subs||[]).map(x=>`<div class="ef-subrow"><input class="sub" value="${esc(x)}"><button class="ef-danger subremove" type="button">×</button></div>`).join('')||'<div class="ef-empty">Nenhuma substituição.</div>'}</div><button class="ef-soft addsub" type="button">＋ Adicionar substituição</button></div></div>`}
function render(){let root=$('#'+ID);if(!root){root=document.createElement('section');root.id=ID;const wrap=$('.wrap')||document.body;wrap.insertBefore(root,wrap.firstChild)}const ids=$$('.meal[id]').map(x=>x.id);const s=load();root.innerHTML=`<div class="ef-card"><h2>🍽️ Editar plano alimentar</h2><div class="ef-info"><b>Plano original totalmente editável.</b> Você pode alterar horários, alimentos, quantidades e as opções de substituição sem perder o que já está cadastrado.</div><div class="ef-list">${ids.map(id=>{const m=current(id);return `<div class="ef-meal"><b>${esc(m?.title||id)}</b><div>${esc(m?.time||'')}</div><div class="ef-actions"><button class="ef-edit" data-edit="${esc(id)}">✏️ Editar refeição</button></div></div>`}).join('')}</div><div class="ef-editor" id="ef-editor"></div></div>`;$$('[data-edit]',root).forEach(b=>b.onclick=()=>open(b.dataset.edit));}
function open(id){const m=current(id);if(!m)return;const e=$('#ef-editor');e.classList.add('show');e.dataset.id=id;e.innerHTML=`<div class="ef-top"><label>Nome<input id="ef-title" value="${esc(m.title)}"></label><label>Horário<input id="ef-time" type="time" value="${esc(String(m.time).match(/\d\d:\d\d/)?.[0]||'')}"></label></div><div id="ef-foods">${(m.foods||[]).map(food).join('')}</div><div class="ef-actions"><button class="ef-soft" id="ef-add">＋ Novo alimento</button><button class="ef-edit" id="ef-save">💾 Salvar alterações</button><button class="ef-danger" id="ef-cancel">Cancelar</button></div>`;bind(e);e.scrollIntoView({behavior:'smooth',block:'start'})}
function bind(e){$$('.remove',e).forEach(b=>b.onclick=()=>b.closest('[data-food]').remove());$$('.subremove',e).forEach(b=>b.onclick=()=>b.parentElement.remove());$$('.addsub',e).forEach(b=>b.onclick=()=>{const box=b.previousElementSibling;box.insertAdjacentHTML('beforeend','<div class="ef-subrow"><input class="sub" value="" placeholder="Alimento substituto — quantidade"><button class="ef-danger subremove" type="button">×</button></div>');$('.subremove:last-child',box).onclick=ev=>ev.target.parentElement.remove()});$('#ef-add',e).onclick=()=>{$('#ef-foods',e).insertAdjacentHTML('beforeend',food({icon:'🍽️',name:'',qty:'',subs:[]}));bind(e)};$('#ef-save',e).onclick=()=>{const s=load(),id=e.dataset.id,foods=$$('[data-food]',e).map(r=>({icon:$('.icon',r).value.trim()||'🍽️',name:$('.name',r).value.trim(),qty:$('.qty',r).value.trim(),subs:$$('.sub',r).map(x=>x.value.trim()).filter(Boolean)})).filter(x=>x.name);s.meals=s.meals||{};const old=current(id)||{};s.meals[id]={...old,id,title:$('#ef-title',e).value.trim()||old.title,time:$('#ef-time',e).value||old.time,foods};save(s);apply(id,s.meals[id]);render()};$('#ef-cancel',e).onclick=()=>e.classList.remove('show')}
function apply(id,m){const e=document.getElementById(id);if(!e)return;const h=$('.meal-head h2',e);if(h)h.textContent=m.title;const t=$('.time',e);if(t)t.textContent=m.time;const f=$('.foods',e);if(f)f.innerHTML=(m.foods||[]).map(x=>`<article class="food"><div class="food-icon">${esc(x.icon)}</div><div class="food-name">${esc(x.name)}</div><div class="food-qty">${esc(x.qty)}</div>${x.subs?.length?`<button class="swap" data-subs="${esc(JSON.stringify(x.subs))}" onclick="showSubs(this)">Substituir ▾</button>`:'<span class="no-subs">Sem substituições</span>'}</article>`).join('')}
function init(){css();render()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else setTimeout(init,50);
})();