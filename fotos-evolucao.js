(() => {
  "use strict";
  const DB="PlanoAlimentarFotosDB", STORE="fotos", ID="fotosEvolucaoApp";
  let db;
  const open=()=>new Promise((res,rej)=>{if(db)return res(db);const r=indexedDB.open(DB,1);r.onupgradeneeded=()=>{if(!r.result.objectStoreNames.contains(STORE))r.result.createObjectStore(STORE,{keyPath:"id",autoIncrement:true})};r.onsuccess=()=>{db=r.result;res(db)};r.onerror=()=>rej(r.error)});
  const all=async()=>{const d=await open();return new Promise((res,rej)=>{const r=d.transaction(STORE,"readonly").objectStore(STORE).getAll();r.onsuccess=()=>res((r.result||[]).sort((a,b)=>b.createdAt-a.createdAt));r.onerror=()=>rej(r.error)})};
  const esc=v=>String(v??"").replace(/[&<>\"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[c]));
  const today=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`};
  function css(){if(document.getElementById("fotos-style"))return;const s=document.createElement("style");s.id="fotos-style";s.textContent=`#${ID}{max-width:1200px;margin:18px auto;padding:0 20px;font-family:Arial;color:#17352d}.fc{background:#fffdf8;border:1px solid #d8d5c7;border-radius:20px;padding:18px;box-shadow:0 6px 20px #17352d14}.ff{display:grid;grid-template-columns:150px 150px 1fr auto;gap:8px;align-items:end}.ff label{font-size:11px;font-weight:800}.ff input,.ff select{width:100%;padding:9px;border:1px solid #d8d5c7;border-radius:9px}.fb{border:0;border-radius:9px;padding:10px 13px;background:#176b52;color:white;font-weight:800;cursor:pointer}.fhint{margin:8px 0 0;color:#60746c;font-size:12px}.fg{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-top:14px}.fi{border:1px solid #dedacf;border-radius:12px;overflow:hidden;background:white}.fi img{width:100%;aspect-ratio:3/4;object-fit:cover}.fm{padding:9px;font-size:12px}.fr{margin-top:7px;border:0;border-radius:8px;padding:7px;background:#f5dfda;color:#8b3e31;font-weight:800;cursor:pointer}@media(max-width:700px){#${ID}{padding:0 10px}.ff,.fg{grid-template-columns:1fr 1fr}.ff button{grid-column:1/-1}}`;document.head.appendChild(s)}
  async function render(){let box=document.getElementById(ID);if(!box){box=document.createElement("section");box.id=ID;(document.querySelector(".wrap")||document.body).appendChild(box)}const ps=await all();box.innerHTML=`<div class="fc"><h2>📷 Fotos de evolução</h2><p>Registre e acompanhe suas fotos de evolução neste dispositivo.</p><form class="ff" id="f-form"><div><label>Data</label><input id="f-date" type="date" value="${today()}"></div><div><label>Tipo</label><select id="f-type"><option>Frente</option><option>Lateral</option><option>Costas</option><option>Outro</option></select></div><div><label>Foto da galeria</label><input id="f-file" type="file" accept="image/*"></div><button class="fb" type="submit">Salvar foto</button></form><div class="fhint">📱 No celular, toque em “Escolher arquivo” para selecionar uma foto já existente na galeria. Não é necessário tirar uma foto na hora.</div>${ps.length?`<div class="fg">${ps.map(p=>`<article class="fi"><img src="${URL.createObjectURL(p.blob)}" alt="Foto de evolução"><div class="fm"><b>${esc(p.tipo)}</b><br>${esc(p.data)}<br><button class="fr" data-id="${p.id}">Remover</button></div></article>`).join("")}</div>`:`<p>Nenhuma foto registrada ainda.</p>`}</div>`;document.getElementById("f-form").onsubmit=async e=>{e.preventDefault();const f=document.getElementById("f-file").files[0];if(!f)return alert("Selecione uma foto da galeria.");const d=await open();const tx=d.transaction(STORE,"readwrite");tx.objectStore(STORE).add({data:document.getElementById("f-date").value,tipo:document.getElementById("f-type").value,blob:f,createdAt:Date.now()});tx.oncomplete=render};box.querySelectorAll("[data-id]").forEach(b=>b.onclick=()=>{if(!confirm("Remover esta foto?"))return;open().then(d=>{const tx=d.transaction(STORE,"readwrite");tx.objectStore(STORE).delete(Number(b.dataset.id));tx.oncomplete=render})})}
  function start(){css();render()}
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",start,{once:true});else start();
})();